# Завершенные этапы
* **✅ Чтение метаданных из .md**: Теперь в начале статьи выводятся авторы, дата и теги (router.js).
* **✅ Умный поиск (Fuse.js)**: Внедрен Fuse.js, генерируется единый индекс `search_index.json` с контентом всех статей. Поиск прощает опечатки.
* **✅ Правое меню с вкладками**: Реализован переключатель «Урок / Связи / Граф» в правом сайдбаре.
* **✅ Сборщик ссылок**: Автоматический парсер ссылок из текста текущей статьи (внутренние и внешние).
* **✅ Интерактивный Граф знаний**: Внедрен ForceGraph, скрипт сборки генерирует `graph.json` на основе связей между статьями.
* **✅ Автоматическая генерация манифестов**: Скрипт сам сканирует папку `articles/` и генерирует `tracks.json`, `cheats.json`, `portfolio.json`, `search_index.json` и `graph.json`.
* Этап 1: Базовая структура и парсинг Markdown
* Этап 2: Система треков, манифесты и CI/CD
* Этап 3: Умный локальный поиск (Базовая версия)
* Этап 4: Собственная In-Browser CMS (Загрузка медиа, транслитерация, предпросмотр карточек)
* Этап 5: Архитектура 2.0 (Node.js Build, No Inline JS, 3-column Layout)

---

# Планы на будущее (Backlog)
* **Аналитика прогресса**: Добавить LocalStorage чек-боксы "Урок прочитан" с визуализацией прогресса в треке.
* **Темная тема для кода**: Улучшить интеграцию Highlight.js с переключателем тем.
* **Экспорт в PDF**: Добавить кнопку печати/экспорта статьи в красиво оформленный PDF.
